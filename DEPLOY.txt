═══════════════════════════════════════════════════════
 NEUROBOT — DEPLOY CHEAT SHEET (GitHub Pages)
═══════════════════════════════════════════════════════

PUSH to GitHub, then enable Pages:

  Repo → Settings → Pages
    Source: Deploy from a branch
    Branch: main  ·  Folder: /(root)  → Save

  Site goes live at: https://<user>.github.io/<repo>/

SHIP these files (everything in the repo root):
  index.html
  logo.svg
  styles.css
  styles.polish.css
  styles.perf.css
  js/                 (whole folder — all 11 files)

DO NOT SHIP:  tests/  .env*  .git*

No PHP, no server, no build step — AI calls NVIDIA
directly from the browser (CORS is allowed by NVIDIA).

───────────────────────────────────────────────────────
 AFTER DEPLOY — 30-SECOND CHECK
───────────────────────────────────────────────────────

1) Site loads?   https://<user>.github.io/<repo>/
   (stale look? Ctrl+Shift+R)

2) AI answers?   open the site, press ` , type:  aitest
   Expect: "✓ AI connection OK (direct) — model replied: OK"

   Offline test also works: open index.html straight from
   disk (file://) — everything runs locally.

───────────────────────────────────────────────────────
 KEY FACTS
───────────────────────────────────────────────────────
 • AI key is embedded in js/ai.js (DEFAULT_KEY). To swap
   it: edit that one line, or paste your own key in
   Settings → AI Connection (stored on your device only).
 • Model switch: Settings → AI Connection, or terminal
   `model gpt` / `model nemotron` (saved per device).
 • All data in localStorage on the device. Export/Import
   in Settings → Data.
 • Terminal: ` key · help · py 6*7 · ask <question>
 • Palette: Ctrl+K · Shortcuts: ?
 • Crafted by TANISHQ LALWANI
═══════════════════════════════════════════════════════
