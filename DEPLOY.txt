═══════════════════════════════════════════════════════
 NEUROBOT — DEPLOY CHEAT SHEET (GitHub Pages)
═══════════════════════════════════════════════════════

LIVE SITE:  https://neurobot1s.github.io/terminal-brain/#/

PUSH to GitHub, then Pages is already configured:
  Repo → Settings → Pages
    Source: Deploy from a branch
    Branch: main  ·  Folder: /(root)

  Every push to main redeploys automatically in 1–2 min.

SHIP these files (everything in the repo root):
  index.html
  logo.svg
  styles.css
  styles.polish.css
  styles.perf.css
  js/                 (whole folder — all 11 files)

DO NOT SHIP:  tests/  .env*  node_modules/  .git*

No PHP, no server, no build step — AI calls go from the browser to
NVIDIA's API (integrate.api.nvidia.com) with automatic relay fallback:
  1. your own relay (Settings → AI Connection, optional — see
     nvidia-relay.js for a free 2-minute Cloudflare Worker deploy)
  2. direct call
  3. public CORS relays (no setup needed, works on GitHub Pages)
The first route that answers is remembered for next time.

───────────────────────────────────────────────────────
 AFTER DEPLOY — 30-SECOND CHECK
───────────────────────────────────────────────────────

1) Site loads?   https://neurobot1s.github.io/terminal-brain/#/
   (stale look? Ctrl+Shift+R to bypass cache)

2) AI answers?   open the site, press ` , type:  aitest
   Expect: "✓ AI connection OK (NVIDIA → nvidia/… via …)"
   If it says no key: Settings → AI Connection → paste a
   FREE key from build.nvidia.com (starts with nvapi-,
   stored on-device only).

   Offline check: open index.html straight from disk
   (file://) — everything except Ask runs locally.

───────────────────────────────────────────────────────
 KEY FACTS
───────────────────────────────────────────────────────
 • AI models: NVIDIA Nemotron only, via NVIDIA's own API
   (default: nemotron-3.5-lightning-30b-a3b). No key is
   embedded — paste your free key once in Settings →
   AI Connection or terminal:  key nvapi-…
 • Model switch: Settings → AI Connection, or terminal
   `model lightning` / `model super` / `model nano`
   (saved per device).
 • Relay (optional, for max reliability): deploy
   nvidia-relay.js to Cloudflare Workers and paste the URL
   in Settings → AI Connection.
 • All data in localStorage on the device. Export/Import
   in Settings → Data.
 • Terminal: ` key · help · py 6*7 · ask <question>
 • Palette: Ctrl+K · Shortcuts: ?
 • Crafted by TANISHQ LALWANI
═══════════════════════════════════════════════════════
